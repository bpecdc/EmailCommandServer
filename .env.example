# Configuration du serveur DailyOrganizer Email Command

# Port du serveur (défaut: 3000)
PORT=3000

# Clé API Mailgun (pour vérifier les signatures des webhooks)
# Trouvable dans Mailgun Dashboard > Settings > API Security
MAILGUN_API_KEY=your-mailgun-api-key-here

# Clé API pour l'application iOS (générez une clé aléatoire sécurisée)
# Utilisez: openssl rand -hex 32
IOS_API_KEY=your-random-ios-api-key-here

# Liste des expéditeurs autorisés (séparés par des virgules)
# Seuls ces emails pourront envoyer des commandes
AUTHORIZED_SENDERS=votre.email@gmail.com,autre.email@domain.com
